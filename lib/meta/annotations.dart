class Private {
  const Private([String message = '']);
}

class PrivateExposed {
  const PrivateExposed([String message = '']);
}

class DynamicParameterType {
  const DynamicParameterType([
    String parameter = '',
    String type = '',
  ]);
}

class DynamicReturnType {
  const DynamicReturnType([
    String type = '',
  ]);
}
