name: tedious_dart
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.3 <3.0.0'

dependencies:
  path: ^1.8.0
  # eventify: ^1.0.1
  buffer:
  logger:
  # charset_converter:
  # node_interop:
    # path: ./node_interop-2.1.0
  sprintf:
  crypto:
  convert:
  pointycastle:
  dcache:
  # js:
  # ts_definition:
  events_emitter:
  # magic_buffer: ^0.0.15
  magic_buffer_copy:
    path: c:/users/kareemzaher/desktop/magic_buffer_copy
  
dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
